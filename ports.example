# Docker Compose Port Configuration
# Create a .env file with these variables to customize ports

# API Port (default: 8080)
API_PORT=8080

# Nginx Port for serving media files (default: 8081) 
NGINX_PORT=8081 